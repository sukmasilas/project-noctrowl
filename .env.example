# Template only — copy to .env and fill in real values there. .env is gitignored; this file is not.
# Never paste real secrets into chat or commit them here.

# --- PostgreSQL (needed starting milestone 2 — core ledger engine) ---
# Format: postgresql://<user>:<password>@<host>:<port>/<database>
# Use a dedicated app-specific Postgres user/database — not the postgres superuser.
#
# Local dev (milestone 2, now): point this at a local Postgres (or local Docker
# container) while Builder is iterating on the schema/ledger engine. This does
# NOT need to be the DigitalOcean droplet yet.
#   e.g. DATABASE_URL=postgresql://noctrowl_app:password@localhost:5432/noctrowl
#
# Deployed on the droplet (milestone 4/5, later): the app and Postgres both run
# on the droplet itself, so the value is still "localhost" from the app's own
# point of view — same variable, different environment, not a different shape.
#   e.g. DATABASE_URL=postgresql://noctrowl_app:password@localhost:5432/noctrowl
#
# Do NOT expose Postgres's port (5432) to the public internet to connect to it
# remotely from elsewhere — DigitalOcean's default firewall blocking it is
# correct and should stay that way. If you ever genuinely need remote access
# (e.g. from a local machine, not the droplet itself), use an SSH tunnel
# rather than opening the port.
DATABASE_URL=postgresql://user:password@localhost:5432/noctrowl

# --- Google Cloud (needed starting milestone 3 — manual data ingestion via Drive) ---
# Drive API scope ONLY. Sheets API is not needed — Sheets is not part of this architecture.
#
# Service-account credentials (legacy/read-only path — kept working, no longer the
# default once OAuth below is configured). Found 2026-09-01: a service account gets
# ZERO storage quota on a personal Gmail Drive outside a Shared Drive (which requires
# paid Google Workspace), so it can list/download files in the real "Finance &
# Accounting" folder but cannot create folders or upload files there (403
# storageQuotaExceeded, confirmed against the live Drive API). Superseded by the
# OAuth vars below for write access; ingestion/drive_client.py keeps this path
# available in case the project ever moves to Google Workspace + Shared Drives.
GOOGLE_APPLICATION_CREDENTIALS=./secrets/google-service-account.json
GOOGLE_DRIVE_ROOT_FOLDER_ID=

# OAuth user credentials (primary path once set up — grants read AND write under
# the user's own Google account and its own storage quota, e.g. sukmakesumo@gmail.com,
# the account that actually owns the "Finance & Accounting" Drive folder).
# Setup (one-time, done locally by the user — never by an agent, never on the droplet):
#   1. In Google Cloud Console: APIs & Services > Credentials > Create Credentials >
#      OAuth client ID > Application type "Desktop app". Download the resulting
#      client-secret JSON.
#   2. Save it at the path below (or set GOOGLE_OAUTH_CLIENT_SECRET to wherever
#      you saved it).
#   3. Run: python3 scripts/authorize_google_drive.py
#      This opens your browser to sign in and grant access; it saves the resulting
#      token (including a refresh token) to GOOGLE_OAUTH_TOKEN_PATH below. The app
#      refreshes this token automatically afterward — no need to re-run unless the
#      token file is lost or access is revoked.
# Path to the downloaded OAuth "Desktop app" client-secret JSON.
GOOGLE_OAUTH_CLIENT_SECRET=./secrets/google-oauth-client-secret.json
# Path where scripts/authorize_google_drive.py saves the resulting token. Once this
# file exists and this var is set, DriveClient uses OAuth instead of the service
# account for all Drive reads and writes.
GOOGLE_OAUTH_TOKEN_PATH=./secrets/google-oauth-token.json

# --- App login (needed starting milestone 4 — basic single-user login gate) ---
APP_LOGIN_USERNAME=
APP_LOGIN_PASSWORD=
# Signs the session cookie (Flask, chosen for milestone 4 — see docs/design/milestone-4-web-app-design.md).
# A random generated value, not a credential to protect like the ones above, but still
# gitignored/env-var-only rather than hardcoded. Generate with: python3 -c "import secrets; print(secrets.token_hex(32))"
APP_SECRET_KEY=

# --- eBay Developer (DEFERRED — not needed for the prototype) ---
# eBay sales data is a manual CSV upload for now (see CLAUDE.md: Data sources & inputs,
# Build milestones). Leave these blank/commented until the eBay API sync milestone is
# actually scheduled — don't fill in or wire up early.
# EBAY_APP_ID=
# EBAY_CERT_ID=
# EBAY_DEV_ID=
# EBAY_REFRESH_TOKEN=
# EBAY_ENV=production

# --- DigitalOcean droplet ---
# Deliberately no env vars here — the app never reads DigitalOcean credentials
# at runtime, and nothing in this project automates droplet management (no
# DIGITALOCEAN_API_TOKEN, no doctl/Terraform). If that ever changes, add real
# fields here then — don't add them speculatively now.
#
# What you actually need, kept OUTSIDE this file:
#   - Your SSH private key (e.g. ~/.ssh/id_ed25519) — used to log into the
#     droplet directly, not something the app reads.
#   - The droplet's IP address or hostname, for `ssh <user>@<droplet-ip>`.
# Before deployment (milestone 4/5), confirm you can still SSH in with these.
